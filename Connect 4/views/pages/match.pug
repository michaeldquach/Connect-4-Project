doctype html
head
  link(href='https://fonts.googleapis.com/css2?family=Poppins&display=swap' rel='stylesheet')
  style
    include ../../css/styles.css

include ../partials/sidenav

include ../partials/topnav

// Page content
.main
  .container
    .div#parentDiv
      .div#boardDiv
        table#board
        table#replayBoard
      .div#chatDiv
      .div#buttonsDiv
        button#forfeit.tablink Forfeit
        button#replayForward.tablink Forward
        button#replayBackward.tablink Backward
        button#exitReplay.tablink Exit Replay
    .div
      a#current-player
      a#viewingreplay
          
//I am unsure of how to directly use this variable in the external clientside js.
script(type="text/javascript").
  var game = !{JSON.stringify(game)};
  var online = !{JSON.stringify(online)};
  var user = !{JSON.stringify(user)};

script(src="../../javascript/connect4script.js")